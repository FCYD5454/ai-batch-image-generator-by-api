import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // 未來可以在此處添加代理設定，以解決開發時的跨域問題
  // server: {
  //   proxy: {
  //     '/api': 'http://localhost:5000'
  //   }
  // }
}) 